[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dbt-extractor"
version = "0.6.0"
description = "Dummy dbt-extractor to bypass invalid TOML build issues on Python 3.14+"
dependencies = []
